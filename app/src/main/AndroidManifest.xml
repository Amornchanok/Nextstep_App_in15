<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amornchanok.nextstep_app">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="ANDROID.PERMISSION.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".map.MapsActivity"
            android:label="@string/title_activity_maps" /> <!-- <activity android:name=".HomeActivity"> -->
        <!-- <activity android:name=".searchStudio.SearchRoomActivity"> -->
        <!-- <activity android:name=".userRegister.LoginActivity"> -->
        <activity android:name=".introScreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".HomeActivity"/> -->

<!--        <activity android:name=".introScreen.SplashScreenActivity"/>-->
        <activity android:name=".introScreen.IntroScreenActivity"/>

        <activity android:name=".Page_firebase_show_img_100" />
        <activity android:name=".Page_detail" />
        <activity android:name=".Page_room_list" />
        <activity android:name=".Page_room_detail_order" />
        <activity android:name=".Page_room_detail" />
        <activity android:name=".StudioListActivity" />


        <activity android:name=".BookingActivity" />
         <activity android:name=".userRegister.LoginActivity"/>
        <activity android:name=".userRegister.UserRegisterActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".userRegister.MainActivity" />

        <activity android:name=".partnerRegister.PartnerConditionActivity" />
        <activity android:name=".partnerRegister.PartnerRegisterProfileActivity" />
        <activity android:name=".partnerRegister.PartnerRegisterImageActivity" />
        <activity android:name=".partnerRegister.PartnerRegisterOtpActivity" />
        <activity android:name=".studioProfile.StudioProfileActivity" />

        <activity android:name=".HomeActivity" />

        <activity android:name=".calendarBooking.CalendarActivity" />
        <activity android:name=".searchStudio.SearchRoomActivity" />

        <activity android:name=".tabProfile.StudioProfileActivity" />

        <activity android:name=".NotiActivity" />
    </application>

</manifest>